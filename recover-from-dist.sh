#!/bin/bash
# Скрипт для автоматического восстановления .ts файлов из dist/
# ВНИМАНИЕ: Это частичное восстановление, нужна ручная доработка

echo "🔄 Начало восстановления файлов из dist/..."

cd "$(dirname "$0")"

# Создать структуру папок
echo "📁 Создание структуры папок..."
find dist/src -type d | sed 's|dist/src|src|' | xargs mkdir -p 2>/dev/null

# Счетчик
count=0
total=$(find dist/src -name "*.js" -not -name "*.spec.js" | wc -l | tr -d ' ')

echo "📊 Найдено $total файлов для восстановления"
echo ""

# Восстановление будет происходить вручную для каждого файла
# Этот скрипт только показывает структуру

echo "✅ Структура папок создана"
echo "📝 Восстановление файлов нужно делать вручную на основе .js и .d.ts файлов"

