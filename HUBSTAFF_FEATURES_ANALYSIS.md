# 🔍 Анализ функций Hubnity vs Hubstaff (BACKEND)

> **Важно:** Этот анализ фокусируется ТОЛЬКО на backend функциях (API, бизнес-логика, обработка данных, интеграции)

## ✅ Что уже реализовано на BACKEND

### Базовые функции

- ✅ **Аутентификация и авторизация** (JWT, refresh tokens, RBAC)
- ✅ **Управление пользователями** (CRUD API, валидация, безопасность)
- ✅ **Управление компаниями** (multi-tenancy, настройки)
- ✅ **Управление проектами** (CRUD API, архивирование, бюджет)
- ✅ **Трекинг времени** (API для start, stop, pause, resume)
- ✅ **Скриншоты** (API загрузки, обработка изображений, миниатюры)
- ✅ **Активность команды** (API статистики по пользователям и проектам)
- ✅ **Базовая статистика** (API для общего времени, активных пользователей)
- ✅ **WebSocket события** (real-time обновления через Socket.IO)
- ✅ **Кэширование** (Redis для производительности)

---

## ❌ Критически важные функции (отсутствуют)

### 1. **Отчеты и экспорт данных** 🔴 КРИТИЧНО (BACKEND)

**Что нужно на backend:**

- ❌ API эндпоинты для генерации отчетов (`GET /api/reports/time`)
- ❌ Бизнес-логика фильтрации данных (по датам, пользователям, проектам)
- ❌ Генерация PDF отчетов (библиотека: pdfkit, puppeteer)
- ❌ Генерация Excel отчетов (библиотека: exceljs)
- ❌ Генерация CSV отчетов
- ❌ API для экспорта данных (`GET /api/reports/export?format=pdf`)
- ❌ Email сервис для отправки отчетов (Nodemailer, SendGrid)
- ❌ Фоновые задачи для генерации больших отчетов (BullMQ)

### 2. **Биллинг и инвойсы** 🔴 КРИТИЧНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Invoice в Prisma (номер, дата, статус, сумма, клиент)
- ❌ Модель InvoiceItem (time entries, количество часов, ставка, сумма)
- ❌ API для создания инвойсов (`POST /api/invoices`)
- ❌ Бизнес-логика расчета стоимости (часы × hourly rate)
- ❌ API для управления инвойсами (CRUD)
- ❌ API для истории платежей (`GET /api/invoices/payments`)
- ❌ Интеграция с Stripe API (создание платежей, webhooks)
- ❌ Интеграция с PayPal API
- ❌ Генерация PDF инвойсов
- ❌ Webhooks для обработки платежей

### 3. **Утверждение времени (Time Approval)** 🔴 КРИТИЧНО (BACKEND)

**Что нужно на backend:**

- ❌ Добавить поле `approvalStatus` в модель TimeEntry (PENDING, APPROVED, REJECTED)
- ❌ Добавить поля `approvedBy`, `approvedAt`, `rejectionComment` в TimeEntry
- ❌ API для утверждения (`POST /api/time-entries/:id/approve`)
- ❌ API для отклонения (`POST /api/time-entries/:id/reject`)
- ❌ API для массового утверждения (`POST /api/time-entries/bulk-approve`)
- ❌ API для получения записей на утверждение (`GET /api/time-entries/pending`)
- ❌ Бизнес-логика проверки прав (только менеджеры могут утверждать)
- ❌ Email уведомления менеджерам (через email сервис)
- ❌ WebSocket события для real-time уведомлений

### 4. **Задачи (Tasks/Todos)** 🔴 КРИТИЧНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Task в Prisma (название, описание, статус, приоритет, дедлайн, проект, исполнитель)
- ❌ Связь Task с TimeEntry (многие ко многим или один ко многим)
- ❌ API для создания задач (`POST /api/tasks`)
- ❌ API для управления задачами (CRUD)
- ❌ API для привязки задач к time entries (`PATCH /api/time-entries/:id` с taskId)
- ❌ API для изменения статуса задач (`PATCH /api/tasks/:id/status`)
- ❌ API для назначения исполнителей (`PATCH /api/tasks/:id/assign`)
- ❌ Бизнес-логика валидации дедлайнов
- ❌ API для получения задач по проекту (`GET /api/projects/:id/tasks`)

### 5. **Категории и теги** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Category в Prisma (название, цвет, компания)
- ❌ Модель Tag в Prisma (название, цвет, компания)
- ❌ Связь TimeEntry с Category (многие ко многим)
- ❌ Связь TimeEntry с Tag (многие ко многим)
- ❌ API для управления категориями (`/api/categories`)
- ❌ API для управления тегами (`/api/tags`)
- ❌ API для фильтрации time entries по категориям/тегам (`GET /api/time-entries?categoryId=...&tagIds=...`)
- ❌ Бизнес-логика для отчетов по категориям

---

## ⚠️ Важные функции (отсутствуют)

### 6. **Отслеживание приложений и URL** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель AppActivity в Prisma (название приложения, время использования, timeEntryId)
- ❌ Модель UrlActivity в Prisma (URL, домен, время использования, timeEntryId)
- ❌ API для отправки данных о приложениях (`POST /api/activities/apps`)
- ❌ API для отправки данных о URL (`POST /api/activities/urls`)
- ❌ API для получения статистики по приложениям (`GET /api/activities/apps/stats`)
- ❌ API для получения статистики по URL (`GET /api/activities/urls/stats`)
- ❌ Модель BlockedUrl в Prisma (URL/домен, компания)
- ❌ API для управления блокировками (`/api/blocked-urls`)
- ❌ Бизнес-логика проверки блокировок

### 7. **Детекция простоя (Idle Detection)** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ API для отправки данных о активности (`POST /api/activity/heartbeat`)
- ❌ Бизнес-логика определения простоя (последний heartbeat > threshold)
- ❌ Cron job для проверки простоя и автоматической паузы
- ❌ API для настройки времени простоя (`PATCH /api/companies/:id/settings`)
- ❌ Поле `idleThreshold` в модели Company (секунды)
- ❌ WebSocket события для уведомлений о простое
- ❌ Автоматическая пауза time entry при простое (бизнес-логика)

### 8. **Геолокация** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Location в Prisma (latitude, longitude, accuracy, timestamp, timeEntryId)
- ❌ API для отправки геолокации (`POST /api/locations`)
- ❌ Модель Office в Prisma (название, адрес, latitude, longitude, radius, компания)
- ❌ Бизнес-логика проверки нахождения в офисе (расчет расстояния)
- ❌ API для управления офисами (`/api/offices`)
- ❌ API для истории местоположений (`GET /api/locations?timeEntryId=...`)
- ❌ Гео-заборы (geofencing) - бизнес-логика проверки вхождения в радиус

### 9. **Уведомления** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Notification в Prisma (тип, сообщение, статус прочтения, пользователь)
- ❌ Модель NotificationSettings в Prisma (настройки для каждого типа уведомлений)
- ❌ Email сервис (Nodemailer/SendGrid) для отправки email
- ❌ Push уведомления сервис (Firebase Cloud Messaging, OneSignal)
- ❌ API для получения уведомлений (`GET /api/notifications`)
- ❌ API для отметки прочитанными (`PATCH /api/notifications/:id/read`)
- ❌ API для настройки уведомлений (`PATCH /api/notifications/settings`)
- ❌ Бизнес-логика создания уведомлений (при новых записях, утверждениях, дедлайнах)
- ❌ WebSocket события для in-app уведомлений
- ❌ Фоновые задачи для отправки email/push уведомлений (BullMQ)

### 10. **Лимиты времени** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Поля `dailyLimit`, `weeklyLimit` в модели User (часы)
- ❌ Поле `timeLimit` в модели Project (часы)
- ❌ Бизнес-логика проверки лимитов при создании/обновлении time entries
- ❌ API для настройки лимитов (`PATCH /api/users/:id/limits`)
- ❌ API для проверки лимитов (`GET /api/users/:id/limits/status`)
- ❌ Cron job для проверки превышения лимитов
- ❌ Автоматическое создание уведомлений при превышении
- ❌ API для получения статистики по лимитам

### 11. **Бюджеты проектов** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Поле `budget` уже есть в Project, но нужно:
- ❌ Бизнес-логика расчета использованного бюджета (сумма всех time entries × hourly rate)
- ❌ API для получения статуса бюджета (`GET /api/projects/:id/budget-status`)
- ❌ Поле `budgetAlertThreshold` в Project (процент для уведомления, например 80%)
- ❌ Cron job для проверки приближения к лимиту бюджета
- ❌ Автоматическое создание уведомлений при достижении порога
- ❌ Бизнес-логика автоматической остановки time entries при превышении бюджета
- ❌ API для отчетов по бюджету

---

## 📱 Мобильные функции

### 12. **Мобильное API** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Оптимизированные эндпоинты с минимальным payload (`GET /api/mobile/time-entries`)
- ❌ API для синхронизации offline данных (`POST /api/mobile/sync`)
- ❌ Бизнес-логика обработки конфликтов при синхронизации
- ❌ API для batch операций (создание нескольких time entries за раз)
- ❌ Упрощенная авторизация (возможно, PIN код для быстрого входа)
- ❌ API для геолокации (уже описано выше)
- ❌ API для отправки heartbeat (для определения активности)

---

## 🔌 Интеграции

### 13. **Интеграции с внешними сервисами** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Integration в Prisma (тип, credentials, настройки, компания)
- ❌ API для управления интеграциями (`/api/integrations`)
- ❌ Сервис для интеграции с Jira API (синхронизация задач)
- ❌ Сервис для интеграции с GitHub/GitLab API (синхронизация коммитов)
- ❌ Сервис для интеграции с Slack API (отправка уведомлений)
- ❌ Webhook эндпоинты для приема данных из внешних систем (`POST /api/webhooks/:integrationId`)
- ❌ API для настройки webhooks (`/api/webhooks`)
- ❌ OAuth2 flow для авторизации в внешних сервисах
- ❌ Публичное API для сторонних разработчиков (API keys, rate limiting)

---

## 💼 Бизнес функции

### 14. **Клиентский портал** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель Client в Prisma (имя, email, компания, проекты)
- ❌ Отдельная система авторизации для клиентов (JWT с другим scope)
- ❌ API для клиентов (`/api/client/*`) с ограниченными правами
- ❌ API для просмотра отчетов по их проектам (`GET /api/client/projects/:id/reports`)
- ❌ API для утверждения времени (`POST /api/client/time-entries/:id/approve`)
- ❌ API для просмотра счетов (`GET /api/client/invoices`)
- ❌ Бизнес-логика ограничения доступа (только к своим проектам)

### 15. **Payroll (Зарплата)** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель PayrollPeriod в Prisma (период, статус, компания)
- ❌ Модель PayrollEntry в Prisma (пользователь, период, часы, сумма)
- ❌ Бизнес-логика расчета зарплаты (часы × hourly rate)
- ❌ API для генерации payroll (`POST /api/payroll/generate`)
- ❌ API для управления payroll (`/api/payroll`)
- ❌ API для экспорта в CSV/Excel для систем учета зарплаты
- ❌ API для истории выплат (`GET /api/payroll/history`)
- ❌ Интеграция с системами учета зарплаты (API или экспорт файлов)

### 16. **Множественные ставки** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель UserRate в Prisma (пользователь, проект, категория, ставка, период действия)
- ❌ Бизнес-логика определения ставки (приоритет: проект > категория > базовая ставка)
- ❌ API для управления ставками (`/api/user-rates`)
- ❌ Бизнес-логика применения правильной ставки при расчете стоимости time entry
- ❌ История изменения ставок (через модель UserRate с датами)
- ❌ API для получения актуальной ставки (`GET /api/users/:id/rate?projectId=...&categoryId=...`)

---

## 📊 Расширенная аналитика

### 17. **Расширенная аналитика** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ API для данных графиков (`GET /api/analytics/charts?type=hours&period=week`)
- ❌ Бизнес-логика агрегации данных для графиков (часы по дням, проектам, пользователям)
- ❌ API для сравнения периодов (`GET /api/analytics/compare?period1=...&period2=...`)
- ❌ Бизнес-логика прогнозирования (на основе исторических данных)
- ❌ API для анализа продуктивности (`GET /api/analytics/productivity`)
- ❌ API для heat maps активности (`GET /api/analytics/heatmap`)
- ❌ Оптимизированные запросы для больших объемов данных
- ❌ Кэширование результатов аналитики

---

## 🔒 Безопасность и соответствие

### 18. **Аудит и логирование** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ Модель AuditLog в Prisma (действие, пользователь, сущность, изменения, timestamp)
- ❌ Декоратор/интерцептор для автоматического логирования действий
- ❌ Бизнес-логика записи изменений (before/after состояния)
- ❌ API для получения логов (`GET /api/audit-logs`)
- ❌ API для экспорта логов (`GET /api/audit-logs/export`)
- ❌ Фильтрация и поиск по логам
- ❌ Ротация логов (удаление старых записей)

### 19. **Соответствие требованиям** 🟡 ВАЖНО (BACKEND)

**Что нужно на backend:**

- ❌ API для экспорта всех данных пользователя (`GET /api/users/:id/export-data`)
- ❌ Бизнес-логика сбора всех данных пользователя (time entries, screenshots, etc.)
- ❌ API для удаления данных пользователя (`DELETE /api/users/:id/data`) - GDPR right to be forgotten
- ❌ Бизнес-логика безопасного удаления (анонимизация или полное удаление)
- ❌ API для запроса на удаление данных (`POST /api/users/:id/request-deletion`)
- ❌ Логирование всех запросов на удаление данных

---

## 🎯 Приоритеты для реализации

### Фаза 1 (MVP+): Критически важно

1. **Отчеты и экспорт** - без этого сложно использовать систему
2. **Биллинг и инвойсы** - ключевая функция для монетизации
3. **Утверждение времени** - необходимо для контроля качества
4. **Задачи** - базовая функция управления проектами

### Фаза 2: Важные функции

5. Категории и теги
6. Отслеживание приложений/URL
7. Детекция простоя
8. Уведомления
9. Лимиты времени

### Фаза 3: Расширенные функции

10. Геолокация
11. Интеграции
12. Клиентский портал
13. Payroll
14. Расширенная аналитика

---

## 📈 Текущий прогресс

**Реализовано:** ~40% базовых функций  
**Критически важно:** 0%  
**Важно:** 0%  
**Расширенные:** 0%

**Общий прогресс:** ~15-20% от полного функционала Hubstaff
