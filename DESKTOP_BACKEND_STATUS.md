# 📊 Статус Backend для Desktop Приложения Hubnity

## ✅ Что РЕАЛИЗОВАНО (готово для desktop)

### 1. **Аутентификация и авторизация** ✅ 100%

- ✅ JWT токены (access + refresh)
- ✅ Авторизация через Bearer token
- ✅ Refresh token механизм
- ✅ Выход из системы (logout)
- ✅ Смена пароля

**API:**

- `POST /api/auth/login`
- `POST /api/auth/refresh`
- `POST /api/auth/logout`
- `GET /api/auth/me`

### 2. **Трекинг времени (Time Tracking)** ✅ 100%

- ✅ Создание записей времени
- ✅ Запуск/пауза/возобновление/остановка таймеров
- ✅ Обновление записей времени
- ✅ Получение активных записей
- ✅ Получение всех записей пользователя
- ✅ Удаление записей

**API:**

- `POST /api/time-entries` - создать запись
- `GET /api/time-entries/my` - мои записи
- `GET /api/time-entries/active` - активные записи
- `GET /api/time-entries/:id` - получить запись
- `PUT /api/time-entries/:id/start` - запустить
- `PUT /api/time-entries/:id/pause` - поставить на паузу
- `PUT /api/time-entries/:id/resume` - возобновить
- `PUT /api/time-entries/:id/stop` - остановить
- `PATCH /api/time-entries/:id` - обновить
- `DELETE /api/time-entries/:id` - удалить

### 3. **Скриншоты (Screenshots)** ✅ 100%

- ✅ Загрузка скриншотов (base64)
- ✅ Автоматическое создание миниатюр
- ✅ Получение скриншотов для записи времени
- ✅ Удаление скриншотов
- ✅ Настройки интервала скриншотов на уровне компании

**API:**

- `POST /api/screenshots` - загрузить скриншот
- `GET /api/screenshots/time-entry/:timeEntryId` - получить скриншоты
- `DELETE /api/screenshots/:id` - удалить скриншот
- `GET /api/companies/screenshot-settings` - настройки скриншотов
- `PATCH /api/companies/screenshot-settings` - обновить настройки

**Особенности:**

- Поддержка base64 формата
- Максимальный размер: 50MB
- Автоматическое создание миниатюр
- Хранение в файловой системе

### 4. **Детекция простоя (Idle Detection)** ✅ 100%

- ✅ Heartbeat API для отправки сигналов активности
- ✅ Автоматическая пауза при простое
- ✅ Cron job для проверки простоя (каждую минуту)
- ✅ Настройки порога простоя на уровне компании
- ✅ WebSocket события для уведомлений

**API:**

- `POST /api/idle/heartbeat` - отправить heartbeat
- `GET /api/idle/status` - получить статус активности
- `GET /api/companies/idle-detection-settings` - настройки детекции
- `PATCH /api/companies/idle-detection-settings` - обновить настройки

**Логика работы:**

1. Desktop приложение отправляет heartbeat каждые 30-60 секунд
2. Сервер обновляет `UserActivity.lastHeartbeat`
3. Cron job каждую минуту проверяет простоя
4. Если `(now - lastHeartbeat) > idleThreshold` → автоматическая пауза
5. Отправляется WebSocket событие `idle:detected`

### 5. **WebSocket (Real-time обновления)** ✅ 100%

- ✅ Подключение через Socket.IO
- ✅ Авторизация через JWT токен
- ✅ Комнаты по компаниям и пользователям
- ✅ Real-time события для всех операций

**События:**

- `activity:new` - новая активность (start, stop, pause, resume)
- `time-entry:update` - обновление записи времени
- `stats:update` - обновление статистики
- `idle:detected` - обнаружен простой
- `user:connected` - пользователь подключился
- `user:disconnected` - пользователь отключился
- `screenshot-settings:update` - обновление настроек скриншотов

**Подписка:**

```javascript
socket.join(`user:${userId}`);
socket.join(`company:${companyId}`);
```

### 6. **Управление проектами** ✅ 100%

- ✅ Получение списка проектов
- ✅ Получение активных проектов
- ✅ Создание/обновление/удаление проектов

**API:**

- `GET /api/projects` - все проекты компании
- `GET /api/projects/active` - активные проекты
- `GET /api/projects/:id` - получить проект
- `POST /api/projects` - создать проект
- `PATCH /api/projects/:id` - обновить проект
- `DELETE /api/projects/:id` - удалить проект

### 7. **Управление пользователями** ✅ 100%

- ✅ Получение профиля пользователя
- ✅ Обновление профиля
- ✅ Получение списка пользователей компании

**API:**

- `GET /api/users/me` - мой профиль
- `PATCH /api/users/me` - обновить профиль
- `GET /api/users` - список пользователей
- `GET /api/users/:id` - получить пользователя

### 8. **Статистика и активность команды** ✅ 100%

- ✅ Статистика по времени
- ✅ Активность команды
- ✅ Кэширование через Redis

**API:**

- `GET /api/team-activity` - активность команды
- `GET /api/time-entries/activities` - активность по записям времени

---

## ❌ Что ОТСУТСТВУЕТ (важно для desktop)

### 1. **Отслеживание приложений (App Tracking)** ❌ КРИТИЧНО

**Что нужно:**

- ❌ Модель `AppActivity` в Prisma
- ❌ API для отправки данных о приложениях (`POST /api/activities/apps`)
- ❌ API для получения статистики (`GET /api/activities/apps/stats`)
- ❌ Хранение данных: название приложения, время использования, timeEntryId

**Пример данных:**

```json
{
  "appName": "Visual Studio Code",
  "windowTitle": "index.ts - Hubnity",
  "timeSpent": 3600, // секунды
  "timeEntryId": "uuid"
}
```

### 2. **Отслеживание URL (URL Tracking)** ❌ КРИТИЧНО

**Что нужно:**

- ❌ Модель `UrlActivity` в Prisma
- ❌ API для отправки данных о URL (`POST /api/activities/urls`)
- ❌ API для получения статистики (`GET /api/activities/urls/stats`)
- ❌ Модель `BlockedUrl` для блокировки сайтов
- ❌ API для управления блокировками (`/api/blocked-urls`)

**Пример данных:**

```json
{
  "url": "https://github.com/user/repo",
  "domain": "github.com",
  "title": "GitHub Repository",
  "timeSpent": 1800, // секунды
  "timeEntryId": "uuid"
}
```

### 3. **Геолокация (GPS Tracking)** ❌ ВАЖНО

**Что нужно:**

- ❌ Модель `Location` в Prisma (latitude, longitude, accuracy, timestamp)
- ❌ API для отправки геолокации (`POST /api/locations`)
- ❌ Модель `Office` для офисов компании
- ❌ API для управления офисами (`/api/offices`)
- ❌ Гео-заборы (geofencing) - проверка нахождения в офисе
- ❌ API для истории местоположений (`GET /api/locations?timeEntryId=...`)

**Пример данных:**

```json
{
  "latitude": 40.7128,
  "longitude": -74.006,
  "accuracy": 10, // метры
  "timeEntryId": "uuid"
}
```

### 4. **Batch операции для синхронизации** ❌ ВАЖНО

**Что нужно:**

- ❌ API для batch создания time entries (`POST /api/time-entries/batch`)
- ❌ API для batch загрузки скриншотов (`POST /api/screenshots/batch`)
- ❌ API для синхронизации offline данных (`POST /api/sync`)
- ❌ Обработка конфликтов при синхронизации

**Пример:**

```json
{
  "timeEntries": [...],
  "screenshots": [...],
  "activities": [...]
}
```

### 5. **Оптимизированные эндпоинты для desktop** ⚠️ ЖЕЛАТЕЛЬНО

**Что нужно:**

- ❌ Минимальный payload для получения данных
- ❌ Поддержка фильтрации на стороне сервера
- ❌ Пагинация для больших списков
- ❌ Сжатие ответов (gzip)

### 6. **Расширенная активность (Keystrokes, Mouse)** ❌ ОПЦИОНАЛЬНО

**Что нужно:**

- ❌ Модель для хранения данных о нажатиях клавиш
- ❌ Модель для хранения данных о движениях мыши
- ❌ API для отправки агрегированных данных
- ⚠️ **Примечание:** Обычно эти данные агрегируются на клиенте и отправляются как часть heartbeat

---

## 📋 Итоговая оценка готовности

### ✅ Готово для базового desktop приложения: **~70%**

**Что можно реализовать СЕЙЧАС:**

1. ✅ Трекинг времени (полный функционал)
2. ✅ Скриншоты (автоматические)
3. ✅ Детекция простоя (heartbeat + автоматическая пауза)
4. ✅ Real-time обновления через WebSocket
5. ✅ Управление проектами и пользователями

**Что НУЖНО добавить для полноценного desktop:**

1. ❌ Отслеживание приложений (App Tracking)
2. ❌ Отслеживание URL (URL Tracking)
3. ❌ Геолокация (GPS Tracking)
4. ❌ Batch операции для синхронизации

---

## 🎯 Рекомендации

### Приоритет 1 (Критично для desktop):

1. **Отслеживание приложений** - без этого desktop приложение теряет основное преимущество
2. **Отслеживание URL** - важно для анализа продуктивности
3. **Batch операции** - необходимо для офлайн режима

### Приоритет 2 (Важно):

4. **Геолокация** - полезно для мобильных сотрудников
5. **Оптимизированные эндпоинты** - улучшит производительность

### Приоритет 3 (Опционально):

6. **Расширенная активность** - можно реализовать позже

---

## ✅ Вывод

**Backend готов для базового desktop приложения на ~70%**

Основные функции (трекинг времени, скриншоты, детекция простоя) реализованы и работают.

Для полноценного desktop приложения нужно добавить:

- Отслеживание приложений и URL
- Геолокацию
- Batch операции для синхронизации

Эти функции можно добавить постепенно, начиная с базового функционала.
