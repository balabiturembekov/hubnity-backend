apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hubnity

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  # Use deployment.yaml + service.yaml (production) or backend.yaml (legacy single-replica)
  - deployment.yaml
  - service.yaml
  # - backend.yaml
  - ingress.yaml

# Для подстановки образа: kustomize edit set image ghcr.io/balabiturembekov/hubnity-backend:TAG
