# 🚀 CI/CD Pipeline для Hubnity

## 📋 Обзор

Настроен полноценный CI/CD pipeline с использованием GitHub Actions для автоматизации тестирования, сборки и деплоя.

## 🔄 Workflows

### 1. **CI Pipeline** (`.github/workflows/ci.yml`)

Запускается при каждом push в `main` или `develop` и при создании Pull Request.

**Этапы:**
- ✅ **Lint & Format Check** - Проверка кода на соответствие стандартам
- ✅ **TypeScript Type Check** - Проверка типов TypeScript
- ✅ **Tests** - Запуск unit-тестов с покрытием кода
- ✅ **Build** - Сборка приложения
- ✅ **Prisma Schema Check** - Валидация схемы Prisma
- ✅ **Security Audit** - Проверка уязвимостей в зависимостях

### 2. **CD Pipeline** (`.github/workflows/cd.yml`)

Запускается при push в `main` или создании тега `v*`.

**Этапы:**
- 🐳 **Build and Push Docker Image** - Сборка и публикация Docker образа в GitHub Container Registry
- 🚀 **Deploy to Staging** - Автоматический деплой в staging окружение
- 🚀 **Deploy to Production** - Деплой в production (только для тегов)

### 3. **PR Checks** (`.github/workflows/pr.yml`)

Специальные проверки для Pull Request с автоматическим комментированием результатов.

**Этапы:**
- Проверка всех этапов CI
- Комментирование PR с результатами тестов и покрытием кода

### 4. **Nightly Build** (`.github/workflows/nightly.yml`)

Запускается каждый день в 2:00 UTC.

**Этапы:**
- Полный набор тестов
- Проверка покрытия кода (threshold: 70%)
- Проверка обновлений зависимостей
- Security audit

## 📦 GitHub Container Registry

Docker образы публикуются в GitHub Container Registry:
- `ghcr.io/<username>/<repository>:latest` - последняя версия из main
- `ghcr.io/<username>/<repository>:<branch>` - версия из ветки
- `ghcr.io/<username>/<repository>:<sha>` - версия по коммиту
- `ghcr.io/<username>/<repository>:v<version>` - версия по тегу

## 🔧 Настройка

### Переменные окружения

Для работы CI/CD необходимо настроить следующие секреты в GitHub:

1. **Для деплоя:**
   - `DEPLOY_SSH_KEY` - SSH ключ для доступа к серверу
   - `DEPLOY_HOST` - IP адрес или домен сервера
   - `DEPLOY_USER` - Пользователь для SSH подключения

2. **Для базы данных (в тестах):**
   - Используется встроенный PostgreSQL в GitHub Actions

### Environments

Настроены два окружения в GitHub:
- **staging** - для тестирования перед production
- **production** - для production деплоя

## 📊 Отчеты

### Code Coverage

Покрытие кода автоматически загружается в Codecov после каждого запуска тестов.

### Security Audit

Результаты проверки безопасности логируются в workflow, но не блокируют деплой.

## 🔄 Dependabot

Настроен автоматический обновление зависимостей:
- **npm** - еженедельно по понедельникам
- **Docker** - еженедельно по понедельникам
- **GitHub Actions** - еженедельно по понедельникам

## 🚀 Использование

### Локальный запуск CI проверок

```bash
# Запустить все проверки локально
npm run ci

# Или по отдельности:
npm run lint:check      # Проверка линтинга
npm run format:check     # Проверка форматирования
npm run type-check       # Проверка типов
npm run prisma:validate  # Валидация Prisma схемы
npm run test:cov         # Тесты с покрытием
npm run build            # Сборка
```

### Ручной запуск деплоя

1. Перейдите в **Actions** → **CD**
2. Нажмите **Run workflow**
3. Выберите окружение (staging/production)
4. Нажмите **Run workflow**

### Создание релиза

1. Создайте тег: `git tag v1.0.0`
2. Запушьте тег: `git push origin v1.0.0`
3. CD pipeline автоматически задеплоит в production

## 📝 Шаблоны

### Pull Request Template

При создании PR автоматически предлагается шаблон с чеклистом.

### Issue Templates

- **Bug Report** - для сообщений о багах
- **Feature Request** - для предложений новых функций

## 🔍 Мониторинг

Все workflow логируются в GitHub Actions и доступны для просмотра:
- История запусков
- Логи каждого этапа
- Артефакты (coverage reports, build artifacts)
- Статус деплоя

## ⚙️ Кастомизация

### Изменение порога покрытия кода

Отредактируйте `.github/workflows/nightly.yml`:
```yaml
THRESHOLD=70  # Измените на нужное значение
```

### Добавление новых этапов

Добавьте новые jobs в соответствующий workflow файл.

### Настройка деплоя

Отредактируйте секции `deploy-staging` и `deploy-production` в `.github/workflows/cd.yml` для добавления команд деплоя.

## 🐛 Troubleshooting

### Тесты не проходят

1. Проверьте логи в GitHub Actions
2. Убедитесь, что база данных доступна
3. Проверьте переменные окружения

### Деплой не работает

1. Проверьте SSH ключи и доступы
2. Убедитесь, что секреты настроены правильно
3. Проверьте логи деплоя

### Docker образ не собирается

1. Проверьте Dockerfile
2. Убедитесь, что все зависимости доступны
3. Проверьте логи сборки

